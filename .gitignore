# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.ipynb_checkpoints/

# Data caches and large derived outputs
reference/
results/weights/*.parquet
results/weights/*.tsv
results/weights/*.txt
results/qc/*.parquet
*.tmp
*.bak

# Raw datasets are large; keep locally only (see README Data section)
Datasets/iPSYCH-PGC_ASD/*.gz
Datasets/ASD_SPARK_iPSYCH_PGC/*.tsv

# LaTeX build artefacts
report/*.aux
report/*.bbl
report/*.bcf
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.run.xml
report/*.synctex.gz
report/*.toc
report/sections/*.aux

# The compiled report and the original proposal are deliverables; track them.
!report.pdf
!proposal.pdf

# Editor
.vscode/
.idea/
